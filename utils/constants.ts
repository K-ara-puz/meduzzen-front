export enum CompanyMemberRoles {
  owner = "owner",
  admin = "admin",
  simpleUser = "user"
}
export enum InviteStatus {
  pending = "pending",
  aborted = "aborted",
  canceled = "canceled",
  approved = "approved"
}
export enum InviteType {
  request = "request",
  invite = "invite"
}